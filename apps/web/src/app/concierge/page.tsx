import { ComingSoon } from '@/components/shell/ComingSoon';

export default function ConciergePage() {
  return (
    <ComingSoon
      eyebrow="Agents under a mandate"
      title="Concierge"
      description="An x402 agent spends the house treasury under its owners' credentials: small tickets auto-pay, large ones queue for approval, and revoking one owner's KYC stops both rails at once."
      note="Coming in the next task — the /api/demo/concierge runtime is already live."
    />
  );
}
